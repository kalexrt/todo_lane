# Engineering Constitution — <project>

> Human-maintained. No frontmatter baseline — update when conventions change, review when onboarding.

## Stack
- Runtime: <runtime>
- Language: <language>
- Framework: <framework>
- DB: <db>
- Test runner: <test runner>

## Conventions
1. <convention> — NOT: <counterexample>
2. <convention — second> — NOT: <counterexample — second>

## Hard Rules
- Never <forbidden thing — reason>
- Always route <flow> through <boundary — reason>

## File Organization
- `src/` → <describe>
- `docs/` → LANE artifacts (specs, tasks, context, ADRs)
